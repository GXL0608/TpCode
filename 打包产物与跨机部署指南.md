# TpCode 打包产物与跨机部署指南

## 1. 这份文档解决什么问题

- 打包后到底会生成几份产物、每份做什么。
- 如何把打包结果拷到另一台机器，从 0 开始部署并启动。
- 如何避免当前仓库在 Windows 下常见的打包坑（路径含空格导致失败）。

---

## 2. 打包后会生成几份文件（先说结论）

`packages/opencode/script/build.ts` 的默认全量构建会生成 **11 份平台包目录**（在 `packages/opencode/dist` 下）：

1. `opencode-linux-arm64`
2. `opencode-linux-x64`
3. `opencode-linux-x64-baseline`
4. `opencode-linux-arm64-musl`
5. `opencode-linux-x64-musl`
6. `opencode-linux-x64-baseline-musl`
7. `opencode-darwin-arm64`
8. `opencode-darwin-x64`
9. `opencode-darwin-x64-baseline`
10. `opencode-windows-x64`
11. `opencode-windows-x64-baseline`

如果使用 `--single`，只会生成 **1 份** 当前机器平台包（推荐你做跨机部署时用这个）。

如果继续执行 `bun run --cwd packages/opencode pack:tgz`，会基于上面的平台包再生成 `.tgz`：

- 全量构建后通常是 **11 个 `.tgz`**。
- 单平台构建后通常是 **1 个 `.tgz`**。

如果是 release 流程（`OPENCODE_RELEASE=1`），还会生成发布压缩包：

- Linux 目标是 `.tar.gz`（6 个）。
- macOS/Windows 目标是 `.zip`（5 个）。
- 合计 **11 个发布压缩包**。

### 每个平台包目录里通常有什么

- `bin/opencode` 或 `bin/opencode.exe`
  - 后端服务与 CLI 主程序（核心可执行文件）。
- `web/`
  - Web 前端静态资源（`packages/app build` 的结果，会被 `opencode web` 直接托管）。
- `package.json`
  - 平台包元数据，供 `bun pm pack` 生成 `.tgz` 使用。

---

## 3. 先决条件（非常重要）

### 3.1 源码路径不要有空格

当前 `build.ts` 在 Windows 下会执行 `cp -R ...`，若仓库路径含空格（例如 `D:\Project location\...`），很容易报错：

- `cp: No such file or directory ...`

建议把仓库放到无空格路径，例如：

- `D:\TpCode`

### 3.2 构建机安装

- Bun `1.3.10`（与根 `package.json` 一致）。
- 正常可用的网络（首次构建会拉取依赖与模型快照）。

---

## 4. 构建机打包步骤（推荐：只打目标机所需 1 份）

以下命令在仓库根目录执行。

### 4.1 安装依赖

```bat
bun install
```

### 4.2 先构建前端静态资源

```bat
bun --cwd packages/app build
```

### 4.3 构建后端可执行包（单平台）

```bat
bun run --cwd packages/opencode build --single
```

如果目标机 CPU 较老（x64 无 AVX2），用基线包：

```bat
bun run --cwd packages/opencode build --single --baseline
```

### 4.4 查看产物目录

```bat
dir /b packages\opencode\dist
```

你会看到 1 个目录，例如：

- `opencode-windows-x64`
- 或 `opencode-windows-x64-baseline`

### 4.5 打成压缩包并拷贝（示例）

```bat
cd packages\opencode\dist
tar -a -c -f opencode-windows-x64.zip opencode-windows-x64
```

把这个 zip 拷到目标机即可。

---

## 5. 目标机从 0 部署（Windows 示例）

> 目标机不需要再安装 Bun（因为已经是编译后的可执行包）。

### 5.1 解压

假设你把包放在：

- `D:\deploy\opencode-windows-x64.zip`

解压后目录类似：

- `D:\deploy\opencode-windows-x64\bin\opencode.exe`
- `D:\deploy\opencode-windows-x64\web\...`

### 5.2 准备数据根目录（强烈建议显式指定）

因为你当前分支构建出的 `OPENCODE_CHANNEL` 不是 `local`，默认共享根会走 `Y:\tpcode`。建议显式覆盖：

- `TPCODE_SHARED_ROOT=D:\tpcode-data`

```bat
mkdir D:\tpcode-data
```

### 5.3 配置环境变量并启动

在 `D:\deploy\opencode-windows-x64` 新建 `start-web.cmd`：

```bat
@echo off
setlocal

REM 数据根（务必配置，避免默认写到 Y:\tpcode）
set TPCODE_SHARED_ROOT=D:\tpcode-data

REM 数据库（务必配置，避免使用代码里的默认种子地址）
set OPENCODE_DATABASE_URL=postgres://tpcode:your_password@127.0.0.1:5432/tpcode

REM 服务鉴权（建议开启）
set OPENCODE_SERVER_USERNAME=tpcode
set OPENCODE_SERVER_PASSWORD=change_me_strong_password

REM 账号与登录（建议显式配置）
set TPCODE_ACCOUNT_JWT_SECRET=replace_with_long_random_secret
set TPCODE_ADMIN_PASSWORD=change_admin_password
set TPCODE_REGISTER_MODE=open

REM 缺本地 web 包时是否允许代理到远端，生产建议关
set OPENCODE_WEB_ALLOW_REMOTE_PROXY=0

REM 如需本地语音转写可开启（按需）
REM set TPCODE_LOCAL_STT_ENABLED=1
REM set TPCODE_LOCAL_STT_MODEL=small
REM set TPCODE_LOCAL_STT_PREWARM=1

bin\opencode.exe web --hostname 0.0.0.0 --port 4096
```

运行：

```bat
start-web.cmd
```

### 5.4 访问

- 本机：`http://127.0.0.1:4096`
- 局域网其他设备：`http://<目标机IP>:4096`

---

## 6. HTTPS（可选，建议）

如果你需要手机端录音/拍照等安全上下文能力，建议按你现有文档《本地HTTPS启动方案.md》接入 Caddy/mkcert，把 `:3443` 反向代理到 `127.0.0.1:4096`。

---

## 7. 验收清单

- `bin/opencode.exe` 能启动且监听在预期端口。
- `http://<ip>:4096` 可打开页面，且接口请求同源成功。
- 登录、会话创建、语音/图片上传链路正常。
- 日志目录和状态目录落在 `TPCODE_SHARED_ROOT` 指定位置。
- 数据库里能看到新写入数据（而不是连到默认种子库）。

---

## 8. 常见故障与处理

### 8.1 `cp: No such file or directory ... packages/app/dist/.`

- 原因：Windows 下构建路径含空格时，`build.ts` 的 `cp -R` 易失败。
- 处理：把仓库放到无空格路径（如 `D:\TpCode`）后重试。

### 8.2 打开页面返回 `web_dist_missing`

- 原因：打包目录里没有 `web` 静态资源，且 `OPENCODE_WEB_ALLOW_REMOTE_PROXY=0`。
- 处理：确认构建机执行过 `bun --cwd packages/app build`，并且产物被带进目标包。

### 8.3 启动后写入了意外目录（如 `Y:\tpcode`）

- 原因：构建通道不是 `local` 时，默认共享根是 `Y:\tpcode`。
- 处理：显式设置 `TPCODE_SHARED_ROOT`。

### 8.4 数据库连接到非预期地址

- 原因：未设置 `OPENCODE_DATABASE_URL` / `OPENCODE_PG_URL` 时会走代码默认值。
- 处理：部署脚本中强制设置 `OPENCODE_DATABASE_URL`。

---

## 9. 你这次最推荐的交付物

如果目标机是 Windows x64，推荐交付这 1 份：

1. `packages/opencode/dist/opencode-windows-x64`（或 `-baseline`）

部署时只需要：

1. 解压该目录。
2. 配置 `start-web.cmd`（环境变量）。
3. 执行 `bin\opencode.exe web --hostname 0.0.0.0 --port 4096`。
