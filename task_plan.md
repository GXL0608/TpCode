# CSHIS 闭环联调任务计划

## 目标

以 `CSHIS` 为真实测试产品，完成从用户选择产品、新建会话、输入简单需求、生成并保存计划，到构建中心执行计划、进入 coding、编译、打包产物的完整闭环验证与修复，直到可以用于正式落地。

## 阶段

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 1. 现状梳理与环境核对 | 进行中 | 核对 CSHIS 配置、正式库联调环境、现有代码入口 |
| 2. 用户侧闭环调试 | 待开始 | 产品选择、会话恢复、新建会话、消息交互、计划生成保存 |
| 3. 管理侧闭环调试 | 待开始 | 构建中心计划展示、执行、状态回显、日志信息 |
| 4. 构建执行闭环调试 | 待开始 | coding、compile、package、产物路径与下载 |
| 5. 回归与交付 | 待开始 | 性能、稳定性、部署包、验收说明 |

## 当前假设

- 本地将优先使用“前端 3008 + 后端 4108 + 正式库 `opencode`”进行联调。
- `CSHIS` 已配置解决方案和编译命令，但真实闭环仍可能存在上下文恢复、计划保存、构建执行等链路缺口。
- 这轮工作以真实操作复现为准，不以单元测试通过代替真机联调。

## 风险与原则

- 不覆盖用户已有修改，所有修复都基于当前工作树增量完成。
- 每复现一个问题，先记录根因，再补测试，再实施修复。
- 若同一问题多次修复失败，暂停继续补丁，改为重构相关模块。
