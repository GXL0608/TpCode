# CSHIS 闭环联调发现

## 当前发现

- 当前工作树存在大量未提交改动，包含用户侧上下文、产品模型、构建链路、SSE、路径映射等多个区域。
- 本轮目标不是单点修复，而是以 `CSHIS` 为真实样本进行闭环验收。
- 本地正式库联调 `3008 -> 4108 -> opencode` 下，`cuichao` 账户可以真实完成：
  - 登录
  - 选择 `CSHIS`
  - 在当前产品页直接发送首条消息
  - 会话返回计划
  - 点击 `Save` 保存计划
- `CSHIS` 当前真实配置存在两个会直接影响构建闭环的风险：
  - 方案 `HIS统一版API` 的 `build_profile.workdirs = ["安统一HISAPI服务"]`，与实际根目录挂载名 `统一HISAPI服务` 不一致。
  - `HIS统一版API` 和 `CSHIS系统` 的 `artifact_include` 都配置为 `dist/**`，但当前编译命令实际把产物写入 `.ai__build`。
- 这说明闭环当前不仅是代码链路问题，还包含真实产品配置问题；需要同时增强后端容错和构建详情回显。

## 待补充

- CSHIS 当前产品配置与解决方案配置
- 本地正式库联调环境的真实行为
- 每个问题的根因、对应修复文件、验证命令与结果
