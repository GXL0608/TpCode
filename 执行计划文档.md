# 医院现场语音/转写/OCR改造可执行计划

## 1. 现状基线（以当前代码与本地数据库为准）

- 前端录音入口：`packages/app/src/components/prompt-input.tsx`
  - 已支持开始/停止录音、基础转写回填。
  - 未支持暂停/继续、实时音量波形、低音量/高噪声提醒。
- 请求组装：`packages/app/src/components/prompt-input/build-request-parts.ts`
  - 语音以 `file` part 发送（含 `duration_ms`、`forModel=false`）。
  - 未带 `raw_transcript/corrected_transcript` 语义字段。
- 后端语音落库：`packages/opencode/src/session/voice.ts`
  - 当前字段语义：`audio_bytes/stt_text/stt_engine`。
- 图片与 OCR：`packages/opencode/src/session/picture.ts`
  - 已有 `ocr_text/ocr_engine`，未做结构化字段提取。
- 消息入库编排：`packages/opencode/src/session/prompt.ts`
  - 已处理 data URL 音频、图片入库分支。
- 本地 `opencode.db`（SQLite）当前表现状：
  - 核心表：`session/message/part` 存在且有数据。
  - `part` 中存在少量图片文件 part；本地样本未出现音频 file part。
  - `session_voice/tp_session_picture` 在本地库未见（代码有 fallback 建表逻辑）。

## 2. 目标与优先级

### P0（立即落地）

1. 弱网适配（本地缓存+自动续传+失败提示）
2. 录音暂停/继续
3. 转写双存（原始转写+用户修正）
4. 图片 OCR 结构化提取

### P1（合规与互通）

1. 敏感信息脱敏与水印
2. 会话全链路核心字段补齐
3. 端间实时同步与断点续作

## 3. 分阶段执行计划

## 阶段 A：数据模型与迁移（先行）

### A1. 语音表扩展

- 目标：
  - 满足 `raw_audio/raw_transcript/corrected_transcript` 双存要求。
  - 为分段与编辑日志预留结构。
- 变更：
  - `packages/opencode/src/session/session.sql.ts` 扩展 `session_voice`：
    - `raw_audio`（bytea/blob，对应原始音频）
    - `raw_transcript`（text）
    - `corrected_transcript`（text）
    - `transcript_segments`（json text，保存 `[start-end]` 分段）
    - `session_trace`（json text，可选，保留终端/网络信息）
  - 更新 `packages/opencode/src/session/voice.ts` fallback `create table if not exists` SQL。
  - 生成迁移目录（PostgreSQL）并同步 SQLite fallback 兼容。
- 验收：
  - 新旧库均可写入语音数据且不破坏历史会话读取。

### A2. 图片表结构化字段扩展

- 目标：
  - OCR 后持久化结构化字段：`device_code/error_code/system_name/problem_desc`。
- 变更：
  - `packages/opencode/src/session/session.sql.ts` 扩展 `tp_session_picture`。
  - `packages/opencode/src/session/picture.ts` fallback SQL 同步。
- 验收：
  - 新上传图片可写入结构化字段；失败时写“未识别”。

## 阶段 B：前端录音能力改造（P0）

### B1. 录音状态机升级（暂停/继续）

- 目标：
  - 支持 `recording -> paused -> recording -> stop`。
- 变更：
  - `packages/app/src/components/prompt-input.tsx`
    - `voicePhase` 扩展 `paused`。
    - 新增 `pauseVoiceInput()`/`resumeVoiceInput()`。
    - UI 按钮状态切换（录音中显示暂停，暂停后显示继续）。
- 验收：
  - 暂停后不丢已录音片段，继续后最终合并为同一语音附件。

### B2. 实时音量波形与环境弱提醒

- 目标：
  - 录音时展示波形。
  - 低音量/高噪声时弱提醒，不阻断录音。
- 变更：
  - 在 `prompt-input.tsx` 接入 `AudioContext + AnalyserNode`：
    - 每帧计算 RMS/峰值，渲染简易波形条。
    - 连续阈值触发提示：
      - “当前录音环境噪音较大，可能影响转写效果”
      - “当前音量过轻，可能影响转写效果”
- 验收：
  - 常规办公噪声与静音环境均可稳定触发正确提醒。

### B3. 文件命名规则落地

- 目标：
  - 文件命名统一为：`session_id_用户ID_时间戳.扩展名`。
- 变更：
  - 前端 `voiceFilename()` 与后端 `audioFilename()` 统一策略。
  - 后端兜底重命名，防止客户端漏传。
- 验收：
  - 所有新语音记录均符合命名规范，且不改变原格式（mp3/amr/webm 等保留原 mime）。

## 阶段 C：转写与编辑闭环（P0）

### C1. 原始转写落库

- 目标：
  - 录音结束后写入 `raw_transcript`。
- 变更：
  - `packages/opencode/src/session/prompt.ts` 在 `SessionVoice.saveDataFile` 写入路径中：
    - 将当前转写文本作为 `raw_transcript`。
    - `raw_audio` 存原始字节（与 `audio_bytes` 一致，后续可迁移为单字段）。
- 验收：
  - 新语音均可查询到 raw transcript。

### C2. 修正文本与编辑日志

- 目标：
  - 用户编辑后保存 `corrected_transcript`，并记录编辑日志（编辑人、时间、前后内容、session_id）。
- 变更：
  - 在 `PATCH /session/:sessionID/message/:messageID/part/:partID` 关联更新语音记录。
  - 新增日志表（建议：`tp_transcript_edit_log`）或复用 `tp_audit_log` 扩展 action/detail。
- 验收：
  - 每次编辑都有可追溯日志，且与会话绑定。

### C3. 语音停顿分段（>=1.5 秒）

- 目标：
  - 自动分段并生成 segment id，关联同一 `session_id`。
- 变更：
  - 优先在后端转写链路（local whisper/模型输出）做时间段标准化：
    - 以 segment 时间差 >= 1.5s 断段。
    - 生成 `seg_<part_id>_<index>` 标识，写入 `transcript_segments`。
- 验收：
  - 转写结果可按段查看，段内含起止秒。

## 阶段 D：图片上传与 OCR 结构化（P0）

### D1. 上传前质量校验

- 目标：
  - 检测模糊度、亮度、完整性；不达标时提示重拍。
- 变更：
  - 前端上传入口（附件模块）增加图像质量检查函数：
    - 拉普拉斯方差（模糊）
    - 亮度均值/直方图（过暗过亮）
    - 边界占比（截断粗检）
- 验收：
  - 低质量图片被拦截并提供“重拍”操作。

### D2. OCR 结构化提取

- 目标：
  - 从 OCR 文本提取结构化字段。
- 变更：
  - `packages/opencode/src/session/picture.ts`
    - 增加 `extractStructuredFields(ocr_text)`：
      - 先正则规则（设备编号/报错码/系统名）
      - 再模型兜底（返回 JSON）
    - 失败字段写“未识别”。
- 验收：
  - 结构化字段可用于检索和统计，不再只保存纯文本。

## 阶段 E：弱网与多端互通（P1）

### E1. 弱网缓存+重试队列

- 目标：
  - 断网时录音/文本/图片本地缓存，联网后自动续传。
- 变更：
  - `packages/app/src/components/prompt-input/submit.ts`
    - 增加本地发送队列（IndexedDB/本地持久化）。
    - 网络恢复监听 + 指数退避重试。
- 验收：
  - 飞行模式录入后恢复网络可自动上送，失败有明确提示。

### E2. 10 秒内双向同步

- 目标：
  - PC 与移动端会话内容 10 秒内可见。
- 变更：
  - 基于现有 `sync/global-sync` 事件机制补充语音/图片变更事件。
- 验收：
  - 双端新增或修改在 10 秒窗口内收敛。

## 4. 测试与验收清单

### 单元/集成测试

- `packages/opencode/test/session/prompt.test.ts`
  - 语音写入 `raw_audio/raw_transcript/corrected_transcript`
  - 图片写入结构化字段
- `packages/opencode/test/server/session-voice.test.ts`
  - 语音 URL 可回放且 content-type 正确
- `packages/opencode/test/server/session-voice-transcribe.test.ts`
  - 非法音频类型被拒绝，错误语义稳定
- `packages/app/src/components/prompt-input/submit.test.ts`
  - 弱网缓存与恢复续传路径

### 手工验收（医院场景）

1. 录音中来电/切后台后恢复，录音不中断或可续录。
2. 暂停后继续录音，最终一条语音附件可播放完整内容。
3. 嘈杂环境触发弱提醒，但不打断操作流程。
4. 图片低质量触发重拍，清晰图可通过并结构化入库。
5. 编辑转写后可查到修改前后与编辑人时间。

## 5. 发布与回滚策略

- 发布顺序：
  1. 后端 schema + API（向后兼容）
  2. 前端能力开关灰度（按角色/组织）
  3. 全量开启并执行数据回填
- 回滚策略：
  - 前端通过 feature flag 关闭新录音流程。
  - 后端保持新字段可空，不阻塞旧流程读取。

## 6. 里程碑建议（可执行节奏）

1. 第 1 周：阶段 A + B1（迁移、暂停续录）。
2. 第 2 周：阶段 B2 + C（波形、双存、编辑日志、分段）。
3. 第 3 周：阶段 D + E1（OCR 结构化、弱网续传）。
4. 第 4 周：阶段 E2 + 全链路联调、灰度上线。

## 7. 当前阻塞与前置决策

1. 移动端“后台持续录音”在纯浏览器受限，需明确是否采用原生容器能力。
2. 本地 SQLite 与生产 PostgreSQL 的迁移策略需统一（避免字段漂移）。
3. 医疗+IT 白名单词典来源、更新机制与归属团队需先确定。
