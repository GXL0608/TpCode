# TpCode 系统技术栈与学习清单

> 目标：帮助新同学快速理解本仓库的“技术构成 + 学习重点 + 实践顺序”。

## 1. 系统概览

- 仓库形态：`Bun + Turborepo` 的 Monorepo。
- 核心产品形态：
1. CLI/服务端（`packages/opencode`）
2. Web App（`packages/app`）
3. Console 管理端（`packages/console/app`）
4. Desktop 桌面端（`packages/desktop`，Tauri）
5. JS SDK（`packages/sdk/js`）
- 默认主分支：`dev`

## 2. 技术栈总览（按层）

### 2.1 工程与构建层

- 运行时与包管理：`Bun`
- 任务编排：`Turborepo`
- 语言与类型系统：`TypeScript`
- 代码规范与格式：`Prettier`、项目内规则文档（`AGENTS.md`）

### 2.2 后端服务层（核心）

- HTTP 框架：`Hono`
- API Schema / OpenAPI：`hono-openapi`、`zod`
- 实时通信：`SSE`（streamSSE）、`WebSocket`
- 服务入口：`Bun.serve`
- 鉴权与权限：JWT/Bearer、RBAC（角色/权限码）

### 2.3 AI Agent 与工具层（核心差异化）

- AI SDK：`ai` + 多 Provider SDK（OpenAI、Anthropic、Google、Azure、Bedrock 等）
- Agent 机制：`build / plan / general / explore` 等模式
- Tool 系统：内置工具（read/edit/bash/search 等）+ 插件工具 + 自定义工具
- MCP（Model Context Protocol）：`local/remote`，含 OAuth 流程
- LSP 工具集成：代码导航、引用、符号等语义能力

### 2.4 数据与存储层

- ORM：`drizzle-orm`
- 迁移：`drizzle-kit` + 项目 migration
- 主数据库：`PostgreSQL`（`packages/opencode`）
- Console 业务库：`PlanetScale/MySQL`（`packages/console/core`）
- 对象存储/同步相关：Cloudflare `R2` / Durable Object（基础设施层）

### 2.5 前端与客户端层

- Web UI：`SolidJS` + `Vite` + `Tailwind`
- Console：`SolidStart` + `Nitro` + Cloudflare 适配
- 桌面端：`Tauri v2`（前端 TS + 后端 Rust）
- 终端 UI：`@opentui/core` + `@opentui/solid`

### 2.6 云基础设施与部署层

- IaC：`SST`
- 平台：Cloudflare（Worker、StaticSite、Astro、Durable Object）
- 第三方服务：Stripe、PlanetScale、GitHub App 等

### 2.7 SDK 与生态扩展层

- OpenAPI 到 SDK：`@hey-api/openapi-ts`
- SDK 构建脚本：`./packages/sdk/js/script/build.ts`
- 插件扩展：`@opencode-ai/plugin`

## 3. 按模块必须掌握的知识点

### 3.1 后端开发必会

- Hono 路由组织、中间件链、错误处理。
- `zod` 参数校验与响应模型约束。
- SSE/WS 实时事件推送机制与背压/降级思路。
- 鉴权（Bearer）与权限中间件（RBAC）实现。
- Drizzle 的表定义、事务、索引、迁移。
- `Bun.serve` 启动参数、端口与生命周期管理。

### 3.2 Agent/AI 方向必会

- Provider 模型选择与配置优先级（env/config/auth）。
- Agent 模式差异（尤其 `build` 与 `plan` 权限差异）。
- Tool Registry 的装配方式（内置 + 插件 + 自定义目录）。
- MCP 的连接模型（stdio/http/sse）和 OAuth 鉴权流程。
- LSP 工具调用模型（位置参数、可用性检测、权限请求）。

### 3.3 前端开发必会

- SolidJS 组件与响应式心智模型（signal/store/effect）。
- App 的 context/provider 组织方式与路由守卫。
- Console 端 SolidStart 路由与服务端渲染链路。
- 与 SDK 的交互模式（初始化、事件订阅、错误处理）。
- Tailwind 样式体系与 UI 组件复用（`packages/ui`）。

### 3.4 桌面端开发必会

- Tauri 前后端桥接（TS invoke <-> Rust command）。
- 插件能力（通知、剪贴板、深链、更新器等）。
- 跨平台差异处理（Windows/Linux/macOS）。
- Rust 基础：模块组织、异步、错误边界、配置读取。

### 3.5 云与运维必会

- SST 应用组织（stage、secret、link、worker 绑定）。
- Cloudflare Worker 与 Durable Object 的职责边界。
- 发布与环境变量管理、日志与问题排查。
- API/前端多入口部署协作（app/docs/api）。

## 4. 推荐学习顺序（全栈）

### 阶段 1：工程基础（1-2 天）

- 了解根目录 `package.json`、`turbo.json`、`sst.config.ts`。
- 跑通：`opencode` 服务 + `app` 前端。

### 阶段 2：后端主链路（3-5 天）

- 从 `packages/opencode/src/index.ts` 追到 `server/server.ts`。
- 选一个业务路由，完整追踪：入参 -> 鉴权 -> DB -> 返回。

### 阶段 3：AI 与工具体系（4-6 天）

- 学 `provider`、`agent`、`tool`、`mcp` 模块。
- 至少实现一个简单自定义工具并接入 registry。

### 阶段 4：前端主链路（4-6 天）

- 学 `packages/app` 的 context 与 session 页面数据流。
- 做一个小功能（列表/筛选/状态）并补测试。

### 阶段 5：桌面与部署（3-5 天）

- 跑一次 Tauri，本地打通前后端调用。
- 看 `infra/*.ts`，理解 Cloudflare/SST 部署结构。

## 5. 学习过程中的高频注意事项

- 测试不要在仓库根目录执行（根目录有防呆脚本）。
- 变更 SDK 后记得执行：`./packages/sdk/js/script/build.ts`。
- 项目默认分支是 `dev`，对比与 PR 基线以 `dev` 为准。
- 优先用 Bun 运行与构建，避免 Node 版本差异带来的干扰。
- 阅读/改动优先在对应 package 内完成，减少跨包误改。

## 6. 建议的最小实践清单（边学边做）

1. 新增一个只读接口字段并补测试。
2. 新增一个工具并接入 Tool Registry。
3. 前端增加一个小交互并接后端真实数据。
4. 修改一处配置 schema 并验证 `/config`、`/global/config` 行为差异。
5. 跑一次 SDK 生成并确认 `v2/gen` 产物变更。

---

如需继续深入，建议下一步再补一份《按模块源码索引（文件级）》文档，把每个学习点映射到具体文件路径与阅读顺序。
