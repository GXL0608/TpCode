# 豆包 03/18 执行计划（基于现有代码与本地数据库）

## 1. 目标与范围

基于 2026-03-18 的沟通记录与仓库现状，今天到本周的目标是：

1. 修复语音转写链路中的核心回归（转写消失、语音发送被强制手输文字）。
2. 完成并稳定“转写双存 + OCR 结构化提取”闭环。
3. 把已实现的弱网、暂停续录、图片脱敏/水印能力纳入统一验收。
4. 补齐会话隔离与用户-角色-权限-会话关联的管理链路。

## 2. 现状基线（代码 + 本地数据库）

### 2.1 已落地能力（代码已存在）

1. 录音暂停/继续、实时波形、低音量/高噪声提示已实现。
   - `packages/app/src/components/prompt-input.tsx`
2. 弱网本地缓存 + 自动重试队列已实现。
   - `packages/app/src/components/prompt-input/submit.ts`
3. 图片质量校验、自动脱敏、自动水印已实现。
   - `packages/app/src/components/prompt-input/attachments.ts`
4. 后端已支持语音/图片扩展字段（代码层）：
   - `session_voice`：`raw_audio/raw_transcript/corrected_transcript/transcript_segments...`
   - `tp_session_picture`：`device_code/error_code/system_name/problem_desc...`
   - 文件：`packages/opencode/src/session/session.sql.ts`
5. 后端语音/图片入库逻辑存在，文本编辑可回写 `corrected_transcript`。
   - `packages/opencode/src/session/prompt.ts`
   - `packages/opencode/src/server/routes/session.ts`

### 2.2 当前核心问题（与今天反馈一致）

1. 语音发送仍被“无文字拦截”阻断。
   - `submit.ts` 中存在 `voices.length > 0` 且文本为空时直接 toast 返回。
2. 桌面端在浏览器识别失败时不会走服务端转写兜底。
   - `prompt-input.tsx` 里 `shouldUseServerFallback()` 仅对移动端返回 true。
3. `raw_transcript` 可能被“手敲补充文本”污染。
   - 后端语音存储中 `rawText` 优先取首个 text part。
4. 新建会话输入残留风险仍在。
   - Prompt 状态按 `dir + id` 持久化；新建会话（`id` 为空）使用工作区级草稿，未强制清空。
5. 迁移文件与代码模型存在漂移。
   - 已有 migration 的 `session_voice/tp_session_picture` 是早期基础列，新增列主要靠运行时 fallback `alter table`。

### 2.3 本地数据库现状（opencode.db）

1. 表计数：`session=55`，`message=1636`，`part=4497`。
2. 本地库中无 `session_voice`、无 `tp_session_picture`（当前实例尚未创建/写入）。
3. `part` 类型分布：`file` 仅 8 条（`image/png=5`, `application/pdf=3`），`audio/*=0`。

结论：当前本地样本还没有语音结构化落库数据，不能直接支撑“双存效果验证”，必须先完成发送链路修复并生成新样本。

## 3. 今日互动问题映射到改造项

1. 录音暂停/继续：代码已完成，补现场回归（暂停后切出 APP 再返回继续录音）。
2. 音量轻提醒：代码已完成，补“提醒不打断输入”的可用性验收。
3. 转写消失：P0，先修转写触发与发送拦截。
4. 发送后看不到转写：P0，增加会话内可见的“原始转写/修正转写”展示与查询。
5. 新建会话旧内容残留：P0，明确新会话默认清空策略（仅保留显式 handoff）。
6. 用户-角色-权限链路：已具备基础，补“用户详情 -> 当前会话/历史会话”。
7. 项目管理/解决方案打通：P1，建立会话与项目绑定、关键词命中方案推荐。
8. 转写双存 + OCR 结构化：P0，本周主线任务。
9. 敏感信息脱敏 + 水印：代码已有前端实现，转为“全端一致性与审计补强”。

## 4. 优先级与依赖

## P0（2026-03-18 至 2026-03-19）

1. 修复语音发送拦截（允许 voice-only 发送）。
2. 修复桌面端转写兜底策略（浏览器识别失败时调用服务端转写）。
3. 固化转写双存正确性（`raw_transcript` 不再依赖手输文本）。
4. 修复新建会话输入残留（会话隔离）。
5. 补 migration，消除 schema 漂移。

## P1（2026-03-19 至 2026-03-20）

1. OCR 结构化提取增加模型兜底（规则失败时补全字段）。
2. 管理端增加会话关联入口（用户详情可见当前/历史会话）。
3. 项目管理与解决方案联动（会话关联项目、关键词推荐方案）。

## 5. 分阶段执行

## 阶段 A（2026-03-18，P0）

### A1. 语音发送与转写触发修复

1. 移除 voice-only 拦截条件，允许“仅语音”直接发送。
   - 目标文件：`packages/app/src/components/prompt-input/submit.ts`
2. 调整转写兜底：桌面端浏览器转写失败时也调用 `/session/voice/transcribe`。
   - 目标文件：`packages/app/src/components/prompt-input.tsx`
3. 发送后在消息区可见转写文本（至少保证落地为 text part，且可编辑）。

验收：

1. 不输入手工文字，仅录音可发送成功。
2. 发送后可看到转写文本；编辑后可保存。

### A2. 新建会话隔离修复

1. 进入新建会话页时，默认清空工作区草稿（仅保留显式 handoff）。
   - 目标文件：`packages/app/src/pages/session.tsx`
   - 目标文件：`packages/app/src/context/prompt.tsx`
2. 清空内容包括：文本输入、语音附件、图片附件、语音状态。

验收：

1. 从会话 A 点“新建会话”后，不再出现 A 的残留输入。

## 阶段 B（2026-03-19，P0）

### B1. 转写双存口径修正

1. `raw_transcript` 以语音转写结果为准（segment 拼接/ASR 结果），不再优先取首条文本 part。
   - 目标文件：`packages/opencode/src/session/prompt.ts`
2. 保持 `corrected_transcript` 由用户编辑回写。
   - 目标文件：`packages/opencode/src/server/routes/session.ts`
3. 增加查询接口（按 message/part 查询 raw+corrected，供前端展示与管理端排查）。

验收：

1. `raw_transcript` 与音频内容一致。
2. 用户修改后 `corrected_transcript` 正确更新，审计日志可追溯。

### B2. 迁移与数据一致性

1. 新增 migration：把 `session_voice/tp_session_picture` 扩展列写入正式迁移。
2. 保留 fallback，但不再依赖运行时补字段。
3. 补一版回填脚本（若历史存在语音 part，则回填 voice 表）。

验收：

1. 新环境执行 migration 后结构与 `session.sql.ts` 一致。
2. 无 fallback 也能正常写入扩展字段。

## 阶段 C（2026-03-20，P1）

### C1. OCR 结构化兜底与质量闭环

1. 结构化提取由“规则”升级为“规则优先 + 模型兜底”。
   - 目标文件：`packages/opencode/src/session/picture.ts`
2. 对 `未识别` 字段输出原因码，便于后续训练数据筛选。
3. 管理端可检索结构化字段（设备编号/报错码/系统名/问题描述）。

### C2. 用户/角色/会话联动

1. 用户详情增加“当前处理会话”“历史会话列表”入口。
2. 角色包增加“研发排障权限包”预设（查看原始录音、编辑转写；禁删会话）。

## 6. 数据模型目标（最终口径）

### 6.1 语音

1. `raw_audio`：原始音频字节
2. `raw_transcript`：原始转写（可含时间戳分段文本）
3. `corrected_transcript`：用户修正文本
4. `transcript_segments`：分段明细

### 6.2 图片

1. `ocr_text`：OCR 原文（脱敏后）
2. `device_code`
3. `error_code`
4. `system_name`
5. `problem_desc`

## 7. 测试与验收清单

### 7.1 自动化测试补充

1. `packages/app/src/components/prompt-input/submit.test.ts`
   - 新增“voice-only 可发送”用例（替换当前阻断用例）。
2. `packages/opencode/test/server`
   - 新增语音双存与文本编辑回写用例。
   - 新增图片结构化字段写入用例。

### 7.2 手工场景（医院驻场）

1. 录音 -> 暂停 -> 切出 APP 拍照 -> 返回继续 -> 停止发送。
2. 纯语音发送，无手敲文字。
3. 嘈杂/低音量提示出现但不打断。
4. 发送后可见转写，编辑后可追溯。
5. 新建会话无上个会话残留。

## 8. 风险与回滚

1. 风险：放开 voice-only 后，空白误发概率上升。
   - 对策：保留“空音频/无有效语音”校验，不再要求手工文本。
2. 风险：迁移后字段不一致导致写入失败。
   - 对策：灰度发布，先预发验证 migration + 写入链路，再全量。
3. 风险：OCR 模型兜底带来时延。
   - 对策：规则先行，模型仅对缺失字段兜底，设置超时与降级。

## 9. 本次计划输出结论

当前项目并非从零开发，核心能力已实现约 70%-80%；本轮应聚焦“回归修复 + 数据一致性 + 会话隔离”三个 P0 点。  
按本计划执行，可在 2026-03-20 前形成稳定的语音/转写/OCR 数据闭环，并支持后续合规与训练数据建设。
