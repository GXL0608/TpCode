请在当前沙盒工作区内执行“编译打包”流程，并严格遵守以下要求：
1. 根据当前会话的git 提交信息编译打包用于部署的文件成压缩包
2. 展开目录时避免海量输出
3.  包里只保留包含更改内容对应的编译文件 要有修改说明
4.  压缩包放到程序根目录下 ai_build_zip文件夹下
5.  如果有多个解决方案的话 按解决方案名称后缀生成各个包
6.  编译失败如果是代码问题 则将错误返回 让用户继续修改 你不要直接改代码
7.  编译失败如果是缺少依赖等 你自己自动迭代
8.  最终要生成可部署的压缩包 并且清理临时文件
9.  将生成的压缩包的相对路径输出
